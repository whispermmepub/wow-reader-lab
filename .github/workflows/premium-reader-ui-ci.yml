name: Premium reader interactions and verify

on:
  push:
    branches: [feature/premium-a-ui-v28]
    paths:
      - 'scripts/apply_premium_reader_ui.py'
      - '.github/workflows/premium-reader-ui-ci.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch-and-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/premium-a-ui-v28
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Apply premium reader transformation
        run: python scripts/apply_premium_reader_ui.py
      - name: Guard source structure
        shell: bash
        run: |
          set -euo pipefail
          git diff --check
          grep -q 'open_reader_settings' app/src/main/java/com/whisper/wowreader/BookReaderActivity.java
          grep -q 'private void presentReaderSheet' app/src/main/java/com/whisper/wowreader/BookReaderActivity.java
          grep -q 'private void positionSelectionBar' app/src/main/java/com/whisper/wowreader/BookReaderActivity.java
          grep -q 'private void showAnnotations' app/src/main/java/com/whisper/wowreader/BookReaderActivity.java
      - name: Commit transformed reader source
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'WoW Reader Lab CI'
          git config user.email 'actions@users.noreply.github.com'
          git add app/src/main/java/com/whisper/wowreader/BookReaderActivity.java
          if git diff --cached --quiet; then
            echo 'No reader source changes to commit'
          else
            git commit -m 'Polish reader selection notes and annotation sheets'
            git push origin HEAD:feature/premium-a-ui-v28
          fi
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.11.1'
      - name: Install Android 16 SDK
        shell: bash
        run: |
          "$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager" "platforms;android-36"
      - name: Compile and lint
        run: gradle :app:assembleDebug :app:lintDebug --stacktrace
